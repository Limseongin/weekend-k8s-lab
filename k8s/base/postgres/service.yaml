apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: weekend-k8s-lab
  labels:
    app: postgres
spec:
  clusterIP: None
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
